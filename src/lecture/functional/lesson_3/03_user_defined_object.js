const {_, L, C} = require('../lib/fp.js');
const log = console.log;

/**
 * 1. Map, Set
 */

/**
 * 2. Model, Collection
 */

/**
 * 3. Product, Products
 */

/**
 * 4. time
 */

const f = function () {
    return 1;
}

Promise.reject('no').catch(e => log("1", e)).catch(e => log("2", e));